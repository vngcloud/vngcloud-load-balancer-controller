{{- if .Values.gatewayApi.nlb.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: vngcloud-nlb
spec:
  controllerName: gateway.vks.vngcloud.vn/nlb
  description: "VNGCloud NLB - L4 Gateway-API class."
{{- end }}
