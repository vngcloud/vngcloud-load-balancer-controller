---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: loadbalancerconfigs.vks.vngcloud.vn
spec:
  group: vks.vngcloud.vn
  names:
    kind: LoadBalancerConfig
    listKind: LoadBalancerConfigList
    plural: loadbalancerconfigs
    shortNames:
    - lbc
    singular: loadbalancerconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.loadBalancerId
      name: LoadBalancer-Id
      type: string
    - jsonPath: .status.address
      name: Address
      type: string
    - jsonPath: .spec.zoneId
      name: Zone
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancerConfig is the Schema for the loadbalancerconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LoadBalancerConfig
            properties:
              backendSubnetId:
                description: BackendSubnetId is deprecated, use PrivateSubnetId instead
                type: string
              clusterId:
                description: ClusterId is the ID of the cluster where the load balancer
                  will be deployed. It helps in organizing resources.
                type: string
              createCertificates:
                description: CreateCertificates defines certificates to be created
                items:
                  properties:
                    secretName:
                      description: SecretName is the name of the Kubernetes secret
                        containing the certificate
                      type: string
                  required:
                  - secretName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - secretName
                x-kubernetes-list-type: map
              enableAutoscale:
                description: EnableAutoscale enables autoscaling for the load balancer
                type: boolean
              isPoc:
                description: IsPOC indicates if this is a proof of concept deployment
                type: boolean
              listeners:
                description: Listeners defines the array of listeners for the load
                  balancer
                items:
                  description: Listener defines a listener configuration for the load
                    balancer
                  properties:
                    allowedCidrs:
                      description: AllowedCidrs defines the allowed CIDR blocks
                      type: string
                    alpnPolicy:
                      description: |-
                        ALPNPolicy controls ALPN advertisement (e.g., "HTTP2Optional", "HTTP1Only").
                        Only honored on HTTPS / TLS-terminate listeners.
                      type: string
                    certificateAuthorities:
                      description: CertificateAuthorities is a list of certificate
                        authorities for mutual TLS (for L7)
                      items:
                        description: ListenerCertificate defines a certificate associated
                          with a listener, can be referenced by ID, name, or secret
                          name. Must provide at least one.
                        properties:
                          id:
                            description: Id is the ID of the certificate
                            type: string
                          name:
                            description: Name is the name of the certificate
                            type: string
                          secretName:
                            description: SecretName is the name of the Kubernetes
                              secret containing the certificate
                            type: string
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    certificateDefault:
                      description: CertificateDefault is the default certificate for
                        the listener (for L7)
                      properties:
                        id:
                          description: Id is the ID of the certificate
                          type: string
                        name:
                          description: Name is the name of the certificate
                          type: string
                        secretName:
                          description: SecretName is the name of the Kubernetes secret
                            containing the certificate
                          type: string
                      type: object
                    clientCertificateId:
                      description: ClientCertificateId is the client certificate Id
                        for mutual TLS
                      type: string
                    defaultPoolName:
                      description: DefaultPoolName is the name of the default pool
                      type: string
                    description:
                      description: Description is an optional description for the
                        listener
                      type: string
                    insertHeaders:
                      description: InsertHeaders defines headers to insert into requests
                      items:
                        description: InsertHeader defines a header to be inserted
                          in requests
                        properties:
                          headerName:
                            description: HeaderName is the name of the header to insert
                            type: string
                          headerValue:
                            description: HeaderValue is the value of the header to
                              insert
                            type: string
                        required:
                        - headerName
                        - headerValue
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: Name is the name of the listener
                      type: string
                    policies:
                      description: Policies is the list of policies for this listener
                        (for L7)
                      items:
                        description: Policy defines a policy configuration for application
                          load balancer listeners
                        properties:
                          action:
                            description: Action defines the action to take
                            type: string
                          description:
                            description: Description is an optional description for
                              the policy
                            type: string
                          keepQueryString:
                            description: KeepQueryString determines if query string
                              should be kept on redirect
                            type: boolean
                          l7Rules:
                            description: L7Rules is the list of L7 rules for this
                              policy
                            items:
                              description: L7Rule defines a Layer 7 rule for policies
                              properties:
                                compareType:
                                  description: CompareType is how to compare the rule
                                    value
                                  type: string
                                ruleType:
                                  description: RuleType is the type of rule
                                  type: string
                                ruleValue:
                                  description: RuleValue is the value to compare against
                                  type: string
                              required:
                              - compareType
                              - ruleType
                              - ruleValue
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          name:
                            description: Name is the name of the policy
                            type: string
                          position:
                            description: Position is the position/priority of the
                              policy
                            format: int32
                            minimum: 1
                            type: integer
                          redirectHttpCode:
                            description: RedirectHttpCode is the HTTP code to use
                              for redirect
                            format: int32
                            type: integer
                          redirectPoolName:
                            description: RedirectPoolName is the name of the pool
                              to redirect to
                            type: string
                          redirectUrl:
                            description: RedirectUrl is the URL to redirect to (for
                              REDIRECT_TO_URL action)
                            type: string
                        required:
                        - action
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    protocol:
                      description: Protocol is the protocol for the listener
                      type: string
                    protocolPort:
                      description: ProtocolPort is the port number for the listener
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    sslPolicy:
                      description: |-
                        SSLPolicy selects the listener's TLS protocol/cipher policy (vngcloud-defined).
                        Only honored on HTTPS / TLS-terminate listeners.
                      type: string
                    timeoutClient:
                      description: TimeoutClient is the client timeout in seconds
                      format: int32
                      type: integer
                    timeoutConnection:
                      description: TimeoutConnection is the connection timeout in
                        seconds
                      format: int32
                      type: integer
                    timeoutMember:
                      description: TimeoutMember is the member timeout in seconds
                      format: int32
                      type: integer
                  required:
                  - name
                  - protocol
                  - protocolPort
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              loadBalancerId:
                description: LoadBalancerId is managed by the controller
                type: string
              loadBalancerName:
                description: |-
                  General fields for all load balancers
                  LoadBalancerName is the name of the load balancer (only set via annotation)
                type: string
              mergingMode:
                description: |-
                  MergingMode controls how this LBC merges with another LBC when both
                  GatewayClass.parametersRef and Gateway.spec.infrastructure.parametersRef are set.
                  Only honored when this object is referenced by GatewayClass.parametersRef.
                  Defaults to PreferGateway.
                enum:
                - PreferGateway
                - PreferGatewayClass
                type: string
              packageId:
                description: PackageId is the size/package of the load balancer
                type: string
              pools:
                description: Pools defines the array of pools for the load balancer
                items:
                  description: Pool defines a pool configuration for the load balancer
                  properties:
                    algorithm:
                      description: Algorithm is the load balancing algorithm for the
                        pool
                      type: string
                    description:
                      description: Description is an optional description for the
                        pool
                      type: string
                    healthMonitor:
                      description: HealthMonitor defines the health monitor configuration
                        for the pool
                      properties:
                        domainName:
                          description: DomainName is the hostname sent in the HTTP
                            Host header
                          type: string
                        healthCheckMethod:
                          description: HealthCheckMethod specifies how the health
                            check request is made (e.g., GET, TCP)
                          type: string
                        healthCheckPath:
                          description: HealthCheckPath is the path used for HTTP health
                            checks
                          type: string
                        healthyThreshold:
                          description: HealthyThreshold is the number of consecutive
                            successful checks before marking healthy
                          type: integer
                        httpVersion:
                          description: HttpVersion defines which HTTP version to use
                            for HTTP-based health checks
                          type: string
                        interval:
                          description: Interval is the time in seconds between each
                            health check
                          type: integer
                        protocol:
                          description: Protocol is the protocol used for health checks
                          type: string
                        successCode:
                          description: SuccessCode specifies which HTTP codes indicate
                            a healthy response
                          type: string
                        timeout:
                          description: Timeout is the maximum time in seconds to wait
                            for a response
                          type: integer
                        unhealthyThreshold:
                          description: UnhealthyThreshold is the number of consecutive
                            failed checks before marking unhealthy
                          type: integer
                      required:
                      - protocol
                      type: object
                    members:
                      description: Members is the list of members in the pool
                      items:
                        description: PoolMember defines a member of a load balancer
                          pool
                        properties:
                          backup:
                            description: Backup indicates if the member is a backup
                              member
                            type: boolean
                          ip:
                            description: IP is the IP address of the pool member
                            type: string
                          monitorPort:
                            description: MonitorPort is the monitor port of the pool
                              member
                            type: integer
                          name:
                            description: Name is an optional name for the pool member
                            type: string
                          port:
                            description: Port is the port number of the pool member
                            type: integer
                          weight:
                            description: Weight is the weight of the pool member
                            type: integer
                        required:
                        - ip
                        - monitorPort
                        - port
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: Name is the name of the pool
                      type: string
                    protocol:
                      description: Protocol is the protocol for the pool
                      type: string
                    stickiness:
                      description: Stickiness enables sticky sessions for the pool
                      type: boolean
                    tlsEncryption:
                      description: TLSEncryption enables TLS encryption for the pool
                      type: boolean
                  required:
                  - name
                  - protocol
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              privateSubnetId:
                description: |-
                  PrivateSubnetId is the private subnet id for the load balancer (for INTERVPC scheme)
                  This is the subnet where the LB's private interface resides
                type: string
              privateZoneId:
                description: |-
                  PrivateZoneId is the zone of the client subnet for InterVPC load balancer
                  This is required for InterVPC scheme since the client subnet is in a different VPC
                type: string
              scheme:
                description: Scheme defines if the load balancer is internal or external
                enum:
                - Internal
                - Internet
                - InterVPC
                type: string
              subnetId:
                description: SubnetId is the subnet id for the load balancer
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are key-value pairs for load balancer tagging
                type: object
              type:
                description: Type defines the type of load balancer (network or application)
                type: string
              vpcId:
                description: VpcId is the VPC id for the load balancer
                type: string
              zoneId:
                description: ZoneId must be the zone where the load balancer will
                  be created
                type: string
            required:
            - loadBalancerName
            - subnetId
            - type
            - vpcId
            - zoneId
            type: object
          status:
            description: status defines the observed state of LoadBalancerConfig
            properties:
              address:
                description: Address is the DNS name or IP address assigned to the
                  load balancer
                type: string
              conditions:
                description: |-
                  Conditions represent the current state of the LoadBalancerConfig resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Ready": the resource is fully functional and ready to serve traffic
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              createdCertificates:
                description: CreatedCertificates is the list of created certificate
                  IDs
                items:
                  properties:
                    certificateName:
                      description: CertificateName is the name of the created certificate
                      type: string
                    id:
                      description: Id is the ID of the created certificate
                      type: string
                    resourceVersion:
                      description: ResourceVersion is the resource version of the
                        secret at creation time
                      type: string
                    secretName:
                      description: SecretName is the name of the Kubernetes secret
                        for the certificate
                      type: string
                  required:
                  - id
                  - resourceVersion
                  - secretName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              createdListeners:
                description: CreatedListeners is the list of created listener IDs
                items:
                  properties:
                    createdPolicies:
                      description: CreatedPolicies is the list of created policy IDs
                      items:
                        properties:
                          id:
                            description: Id is the ID of the created policy
                            type: string
                        required:
                        - id
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - id
                      x-kubernetes-list-type: map
                    id:
                      description: Id is the ID of the created listener
                      type: string
                    port:
                      description: Port is the port number of the created listener
                      type: integer
                  required:
                  - id
                  - port
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              createdPools:
                description: CreatedListeners is the list of created listener IDs
                items:
                  properties:
                    createdMembers:
                      description: CreatedMembers is the list of created member IDs
                      items:
                        description: PoolMember defines a member of a load balancer
                          pool
                        properties:
                          backup:
                            description: Backup indicates if the member is a backup
                              member
                            type: boolean
                          ip:
                            description: IP is the IP address of the pool member
                            type: string
                          monitorPort:
                            description: MonitorPort is the monitor port of the pool
                              member
                            type: integer
                          name:
                            description: Name is an optional name for the pool member
                            type: string
                          port:
                            description: Port is the port number of the pool member
                            type: integer
                          weight:
                            description: Weight is the weight of the pool member
                            type: integer
                        required:
                        - ip
                        - monitorPort
                        - port
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    id:
                      description: Id is the ID of the created pool
                      type: string
                    name:
                      description: Name is the name of the created pool
                      type: string
                  required:
                  - id
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              createdTags:
                additionalProperties:
                  type: string
                description: CreatedTags is the map of tags created on the load balancer
                type: object
              lastReconcileMessage:
                description: LastReconcileMessage contains a message from the last
                  reconciliation
                type: string
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last reconciliation
                  attempt
                format: date-time
                type: string
              loadBalancerId:
                description: LoadBalancerId is the actual ID of the load balancer
                  in VNG Cloud
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
