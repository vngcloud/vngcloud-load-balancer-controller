---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: listenerruleconfigs.gateway.vks.vngcloud.vn
spec:
  group: gateway.vks.vngcloud.vn
  names:
    kind: ListenerRuleConfig
    listKind: ListenerRuleConfigList
    plural: listenerruleconfigs
    shortNames:
    - lrc
    singular: listenerruleconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ListenerRuleConfig is the Schema for the listenerruleconfigs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ListenerRuleConfigSpec defines the desired state of ListenerRuleConfig.
            properties:
              actions:
                description: Actions, if set, supersede the host rule's default REDIRECT_TO_POOL
                  action.
                items:
                  description: RuleAction overrides the default REDIRECT_TO_POOL action
                    of the host rule.
                  properties:
                    fixedResponse:
                      description: FixedResponse is required when Type is "FixedResponse".
                      properties:
                        body:
                          description: Body of the response.
                          type: string
                        contentType:
                          description: ContentType of the response body. Defaults
                            to text/plain when Body is set.
                          type: string
                        statusCode:
                          description: StatusCode of the synthesized response.
                          format: int32
                          type: integer
                      required:
                      - statusCode
                      type: object
                    redirect:
                      description: Redirect is required when Type is "Redirect".
                      properties:
                        httpCode:
                          description: HTTPCode is the redirect status code (default
                            302).
                          format: int32
                          type: integer
                        keepQueryString:
                          description: KeepQueryString preserves the original query
                            string in the redirect.
                          type: boolean
                        url:
                          description: URL to redirect to.
                          type: string
                      required:
                      - url
                      type: object
                    type:
                      description: Type of action.
                      enum:
                      - FixedResponse
                      - Reject
                      - Redirect
                      type: string
                  required:
                  - type
                  type: object
                type: array
              additionalMatches:
                description: AdditionalMatches are AND'd with the host HTTPRoute rule's
                  standard matches.
                items:
                  description: |-
                    AdditionalMatch is AND'd onto the policy generated by the host HTTPRoute rule.
                    Used to express vngcloud-specific match types not yet covered by core HTTPRoute matches.
                  properties:
                    compare:
                      description: 'Compare values: EQUAL_TO | STARTS_WITH | ENDS_WITH
                        | CONTAINS | REGEX (vngcloud-defined).'
                      type: string
                    name:
                      description: Name is the header / query-parameter name. Unused
                        for Method / SourceIP.
                      type: string
                    type:
                      description: Type of match.
                      enum:
                      - Header
                      - QueryParam
                      - Method
                      - SourceIP
                      type: string
                    value:
                      description: Value to compare against.
                      type: string
                  required:
                  - compare
                  - type
                  - value
                  type: object
                type: array
              position:
                description: |-
                  Position pins policy ordering within the listener. Default is controller-assigned
                  per Gateway-API match-specificity rules.
                format: int32
                type: integer
            type: object
          status:
            description: ListenerRuleConfigStatus defines the observed state of ListenerRuleConfig.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
